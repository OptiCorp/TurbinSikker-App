export type SendingFormValuesEntity = {
    checklistId: string
    userIds: string[]
    creatorId: string
}

export type ListEntity = {
    id: string
    title: string
    value: string
    label: string
}
